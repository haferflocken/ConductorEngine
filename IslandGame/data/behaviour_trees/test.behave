tree TestTree
select
	sequence
		select
			select
				select
					log("deeep")
		log("Hello, world!")
		log("Sequence in action!")
	sequence
		log("Nope, nope, nope!")
	
tree SecondTree
domain
	{ $blackboard_component GetBool "is_alive" }
	log("it's alive!")

tree TestTree
select
	sequence
		select
			select
				select
					log("deeep")
		log("Hello, world!")
		log("Sequence in action!")
	sequence
		log("Nope, nope, nope!")
	
tree SecondTree
domain
	{ 4 < 3 }
	log("it's alive!")
